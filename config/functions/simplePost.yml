handler: src/commands/simplePost.handler
events:
  - http:
      path: simplePost
      method: post
      cors: true
      # authorizer:
      #   arn: ${self:custom.authorizerARN}
      #   resultTtlInSeconds: 0
      #   identitySource: method.request.header.Authorization
      #   type: token
      request:
        schemas:
          application/json: ${file(schemas/simplePost.json)}
